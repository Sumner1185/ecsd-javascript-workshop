// Conditions

let colour = "orange";

if (colour === "green" || colour.toUpperCase() === "ORANGE") {
    console.log(`"Colour is great!"`)
} else if (colour.toUpperCase() === "RED") {
    console.log(`"Colour isn't nice"`)
} else {
    console.log(`"Colour is unknown"`)
}